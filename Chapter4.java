package inClassExe;

public class Chapter4 {
	public static void main(String[] args) {
		//ceiling raises a floating point number to the next integer so
		//math.ceil(2.1) returns 3.0
		//it goes up!
		//math.ceil(-2.1) returns -2.0 because it rounds up! -2.0 is larger than -2.1
		
		// [a,a+b)
		// a + Math.random() * b
		// [50,120)
		// a = 50
		// b = 70
		// 50 + Math.random() * 70;

	}

}
